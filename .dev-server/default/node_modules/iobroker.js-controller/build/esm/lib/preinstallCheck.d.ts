/**
 * we cannot use semver and other dependencies here, because dependencies are
 * not installed yet, so the version checks get a bit messy
 */
export {};
//# sourceMappingURL=preinstallCheck.d.ts.map